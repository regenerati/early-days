module.exports = {
  siteUrl: 'https://your-domain.com', // Change to your actual domain
  generateRobotsTxt: true,
  outDir: './public',
  // Optionally, you can exclude anchor links and only include real pages
  // exclude: ['/404'],
};